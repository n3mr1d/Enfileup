<iframe src="{{ route('fullpage.pastebin', ['uuid' => $uuid]) }}" frameborder="0" width="{{ $width }}"
    height="{{ $height }}" title="{{ $title }}" allowfullscreen
    style="border-radius:8px; border:1px solid #ddd; background:white;" loading="lazy">
    Your browser does not support iframes.
</iframe>
